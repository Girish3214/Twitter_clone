import {  makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
    sideBarContainer: {
        backgroundColor: "red",
        height: "100vh"
    }
}))

export default useStyles