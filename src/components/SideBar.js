import React from 'react'
import useStyles from '../styles/sideBarStyles'

const SideBar = () => {
    const classes = useStyles();
    return (
        <footer className={classes.sideBarContainer}>
            SideBar
SideBar
        </footer>
    )
}

export default SideBar
